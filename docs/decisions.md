# 决策记录（Decision Log）

格式：日期、决策编号、背景、决策、理由。新决策追加到文末；修改历史决策需新增一条并说明原因。

## D-001 模型分层

- 日期：2026-08-15
- 决策：高频机械任务（摘要/提取/整理）用 `qwen3.7-flash-2026-07-15`；规划/分析/成文/审查用 `qwen3.5-omni-plus-2026-03-15`。
- 理由：免费额度覆盖这两个模型；flash 便宜快、omni-plus 质量高；模型 ID 全部走配置文件，便于更换。

## D-002 来源可靠性：三层结构

- 日期：2026-08-15
- 决策：发现层（新闻搜索/RSS 找候选）→ 内容层（白名单域名抓正文）→ 校验层（结论必须挂原文片段+URL）。
- 理由：避免“网上随便扒两篇”；白名单保证来源可信；引用校验防幻觉。

## D-003 发现层首选 Bing News RSS

- 日期：2026-08-15
- 决策：候选发现用 Bing News RSS（cn.bing.com，国内可达）；Google News RSS 不采用。
- 理由：全项目网络必须国内可达、无代理。

## D-004 记忆方案

- 日期：2026-08-15
- 决策：项目级 `AGENTS.md` + `docs/decisions.md` 作为长期记忆；会话间用 `codex resume/fork`；项目内工作台账 JSON（V1.0 实现）作为运行时记忆。
- 理由：长任务与多阶段项目的上下文必须可持续、可追溯。

## D-005 交互模式：阶段化 + 可中断

- 日期：2026-08-15
- 决策：工作流每完成一个阶段暂停汇报，用户可“继续/换方向/补充来源/跳过”。
- 理由：用户需要中途改方向；降低一次跑完全流程的返工风险。

## D-006 网络策略：全国内、无代理

- 日期：2026-08-15
- 决策：所有外部网络（LLM API、搜索、参考检索、包源）使用国内可直达端点；pip/uv 包源用清华镜像。
- 理由：用户环境要求；github.com 作为参考检索主源（无需代理）。

## D-007 发现层最终方案：白名单媒体 RSS 关键词检索

- 日期：2026-08-15
- 背景：原方案（Bing News RSS）实测发现 Bing 新闻/搜索对自动化请求不可靠（返回空 RSS 或纯 HTML 页）。
- 决策：发现层改为在白名单/可信媒体官方 RSS 中按关键词检索（并发抓取、标题+摘要匹配）；36氪（反爬）与虎嗅（超时）待解决后补充。
- 理由：来源可靠优先于覆盖广度；RSS 结构化、国内可达、无反爬问题。
## D-008 V3.0 长期记忆：本地知识库 + 向量检索（RAG）

- 日期：2026-08-15
- 背景：V2.0 每次分析从零开始，历史报告结论无法复用；主对话上下文有限，需要把“读过的内容”沉淀为可检索记忆。
- 决策：新增 `crawler/memory/`（`embed.py` 用 `text-embedding-v3` 生成向量；`store.py` 提供 `KnowledgeStore`，文档分块 800 字/块、100 字重叠、按句号切块，JSON 落盘 `data/knowledge/index.json`，余弦相似度检索并按文档去重）；新增工具 `search_knowledge` 并注册；V2.0 报告生成后自动入库；CLI 支持 `--stats/--index-reports/--add-file/--query`。
- 理由：JSON + 本地向量零额外服务依赖、全国内可达；报告入库后可跨会话复用结论，是长期记忆的第一版。
- 教训：分块函数曾因末块 `start` 回退写成死循环，内存被吃爆触发 `MemoryError`（终端白屏崩溃）；已修复（末块直接截断退出）并加冒烟验证。长文本处理代码必须先小样本验证终止性。
## D-009 V3.1 Web 界面：Gradio 本地服务

- 日期：2026-08-15
- 背景：开源后需要让非开发者也能用，CLI 门槛较高。
- 决策：新增 `crawler/webui.py`（Gradio 6.x），两个页签——分析报告（输入课题→进度条→渲染报告 + 文件路径 + Token 用量）与知识库检索；同时把 `orchestration.py` 的 CLI 主流程抽出为 `run_pipeline(topic, ..., progress=回调)`，CLI 与 Web 共用一条流水线。
- 理由：Gradio 纯 Python、清华镜像可装、对 AI 工作流展示友好；服务只绑定 127.0.0.1，API Key 仅存服务端 `.env`、不下发浏览器。
- 备注：取消/中断（gr.Cancelled）留待后续版本；默认端口 7860。
## D-010 V3.1 收尾：可取消流水线 + 报告下载 + 模型选择

- 日期：2026-08-15
- 决策：`run_pipeline` 增加 `cancelled` 回调（配合 `PipelineCancelled` 异常），资料收集循环与各阶段间轮询取消标志；Web 界面增加“取消任务”按钮、报告下载（`gr.File`）、收集/分析模型下拉选择。
- 理由：长任务需要可中断（对应规划书“智能体陷入无限循环”风险的兜底之一）；下载与选模型提升实际可用性。
- 备注：取消为步骤间生效（单次 LLM 调用不打断）；CLI 行为不受影响。
## D-011 V3.2 报告导出：PDF / Word

- 日期：2026-08-15
- 决策：新增 `crawler/export.py`：轻量 Markdown 块解析（标题/段落/引用/有序无序列表/表格/加粗/链接）→ reportlab 渲染 PDF（优先系统微软雅黑，缺失回退内置 STSong-Light CID 字体）+ python-docx 渲染 Word（中文字体微软雅黑，链接为可点击超链接）；CLI 增加 `--export pdf,docx`，Web 界面增加“导出 PDF/Word”按钮。
- 理由：报告交付常用格式；PDF/Word 纯本地生成、零外部服务依赖；PDF 用系统字体避免在仓库捆绑大字体文件。
- 备注：PDF 链接以蓝色下划线呈现；Word 链接可点击跳转。
## D-012 验收修复：正文抽取、表格转义、导出防御与搜索排序

- 日期：2026-08-15
- 背景：V3.2 验收发现三类问题：①PDF 导出失败——标题含 `|` 导致资料清单表格列数错乱，reportlab 布局报 negative availWidth；②网页正文混入大量导航噪音，研究员难以提炼事实；③RSS 候选仅按日期排序，无关文章淹没有效结果（两次验收报告出现“无事实条目”）。
- 决策：
  1. `crawler/tools/web.py`：正文抽取优先定位内容容器（`article`/常见正文选择器），失败回退整页；
  2. `crawler/multi_agent/orchestration.py`：Markdown 表格单元格中的 `|` 转义为全角 `｜`；
  3. `crawler/export.py`：表格行归一化（补齐列数）+ 显式等分列宽，杜绝负宽度；
  4. `crawler/tools/search.py`：候选按相关度打分排序（原词命中标题 +3 / 正文 +2，2-gram 兜底 +0.5），并把相关度展示给模型参考。
- 理由：保证“来源发现 → 正文抽取 → 报告生成 → PDF/Word 导出”全链路可靠；打分排序让模型优先选取真正相关的报道。
- 备注：同一课题重复生成时，记忆库按 title+source 去重（新报告不再入库），属已知行为，后续可改为带时间戳标题或覆盖更新。
## D-013 选题助手：分析前预检 + Web 页签

- 日期：2026-08-15
- 背景：用户实测“QQ音乐/网易云/汽水音乐”话题失败——白名单媒体（科技/AI 向）不覆盖音乐流媒体领域；完整分析跑完才发现没料，浪费约 5 分钟。
- 决策：新增 `crawler/topic_check.py`（选题预检）：从课题提取实体词（按连接词切分、剔除通用词、公司前缀拆出产品名、母公司名不计相关），复用 `search_candidates` 给出“有料/偏少/没料”判定、相关候选与建议；`run_pipeline` 材料不足时输出诊断建议；Web 新增“选题助手”页签（选题指南 + 预检按钮 + 一键去生成报告）。
- 理由：把失败成本从 5 分钟降到 1 分钟；给用户明确的选题方法论（热点优先、2 对象对比、官方产品名、先预检再开跑）。
- 备注：通用词与母公司名过滤在 `crawler/tools/search.py` 实现（GENERIC_TERMS / BROAD_COMPANIES），同时提升智能体搜索质量。

## D-014 方向 C 第一步：国内可达垂直媒体清单（实测结论）

- 日期：2026-08-15
- 背景：用户实测“QQ音乐/网易云/汽水音乐”类课题失败——科技/AI 向白名单覆盖不了音乐/文娱/消费垂直领域；方向 C 第一步要求先盘点国内可达、无反爬的垂直媒体，且来源必须可靠。
- 决策：逐一实测候选站点后接入：
  1. 发现层新增 3 个 RSS：IT之家（`ithome.com/rss/`，科技/数码）、机核（`gcores.com/rss`，游戏/泛文化）、Solidot（`solidot.org/index.rss`，科技新闻）；
  2. 内容层白名单新增 5 个域名：IT之家、机核、Solidot、娱乐资本论（`ylzbl.com`，文娱/明星商业）、品牌星球（`brandstar.com.cn`，新消费/品牌）；
  3. 明确不接入：机器之心（RSS 返回付费服务页）、亿欧（HTTP 202 反爬拦截）、DoNews/品玩（404）、晚点（SSL 错误）、三声（域名已废弃为直播站）；36氪/虎嗅维持已知不可用，注释保留避免重复踩坑。
- 理由：可靠性优先于覆盖面；新源均国内可直达、无反爬，正文抽取实测干净（娱乐资本论/品牌星球正文容器可正常抽取）。娱乐资本论、品牌星球、机核补齐了音乐/文娱/消费类课题的覆盖缺口。
- 备注：娱乐资本论与品牌星球无官方 RSS，暂只入内容层白名单（用户粘贴链接/其他来源引用时可抓取），自动发现需等“列表页抓取”作为方向 C 后续步骤。

## D-015 方向 C 第二步：无 RSS 站点的列表页发现

- 日期：2026-08-15
- 背景：D-014 接入的娱乐资本论/品牌星球只有内容层白名单，无法被发现层自动检索（实测“白敬亭”等文娱关键词 0 命中）。
- 决策：`crawler/sources.py` 新增 `LIST_PAGES` 配置（站点 → 列表页/链接特征/标题与日期选择器）；`crawler/tools/search.py` 新增 `_match_list_page`：抓取首页列表页，按 `link_match` 提取文章链接，优先用锚内 `h2/h4/.hf-title` 标题，回退锚文本并剔除日期 span 与“· 文章”类栏目噪音，日期归一化为 `YYYY-MM-DD`；`search_candidates` 改为并发抓取 RSS + 列表页并按 URL 去重。
- 已接入：娱乐资本论（`ylzbl.com/`，`/article/`，约 15 条）、品牌星球（`brandstar.com.cn/`，`/news/`、`/in-depth/`、`/brand/`，约 30 条）。
- 理由：无需新增外部服务；实测“白敬亭→娱乐资本论”“李宁/lululemon→品牌星球”均命中，标题干净、日期归一化。
- 备注与已知局限：①品牌星球“精选”列表页（`/news`）为 Nuxt 前端渲染，静态抓取拿不到文章链接，只用了首页；②混合中英文复合实体（如“白敬亭GOODBAI”）暂不拆词，选题助手已提示用户用纯实体名分别搜索，拆词优化留待后续；③cnBeta RSS 近期偶发超时（非本次改动引入）。

## D-016 数据源方向调整：主抓科技+金融，撤下文娱源

- 日期：2026-08-15
- 背景：用户反馈不关注文娱内容，重心转向科技与金融（论文/学术用途暂缓）。D-014/D-015 接入的文娱/消费源（机核/娱乐资本论/品牌星球）未实际使用。
- 决策：
  1. 移除文娱源：RSS 删除机核；列表页删除娱乐资本论/品牌星球；内容层白名单删除 `gcores.com`/`ylzbl.com`/`brandstar.com.cn`。
  2. 列表页接入三大财经源：第一财经（`yicai.com/news`，标题 `h2`）、界面新闻（`jiemian.com/article`，标题 `p`）、21财经（`21jingji.com/article`，标题取 `a[title]` 属性，`_match_list_page` 新增 `title_attr` 配置支持）。
  3. 华尔街见闻确认不可用：首页与 `/articles` 均为前端渲染空壳（静态 HTML 无文章链接）、`/rss` 404、`/feed` 返回 HTML，不接入。
- 理由：与用户内容偏好一致；三大财经源正文抽取实测干净（第一财经/界面有少量导航噪音，21 财经移动版干净）；科技 12 个 RSS 保持不变。
- 备注：科研/论文源（arXiv/OpenAlex/科学网等）等用户启动论文用途时再接入；`search.py` 的 `SPEC` 描述同步更新为“科技 RSS + 财经列表页”。

## D-017 科技+金融细化：领域感知选题 + 相关度打分优化

- 日期：2026-08-15
- 背景：数据源收敛为科技+金融后，实测发现：复合实体词（QQ音乐/豆包大模型）整词匹配不到标题，靠 2-gram 召回噪声大；旧新闻与专业财经源无差异化权重；选题助手无法区分科技/金融选题。
- 决策：
  1. 实体拆词（`search.py _extract_terms`）：中英/数字混合边界拆分（白敬亭GOODBAI → 白敬亭 + GOODBAI）；已知中文领域词切分（豆包大模型 → 豆包 + 大模型）；拆出的 2 字中文品类词与常见英文泛后缀（app/api/ai…）进“弱词”（命中 +0.5），纯数字子词忽略。
  2. 领域判定（`DOMAIN_TERMS` + `domain_of`）：科技/金融词表判断课题领域，选题助手在 verdict 中显示领域（科技/金融/综合/其他）并给针对性建议。
  3. 打分细化：无原词命中的纯 2-gram/弱词分数封顶 1.0；日期衰减（7 天内 ×1.2 / 30 天内 ×1.0 / 更早 ×0.5，支持 RSS pubDate 与 YYYY-MM-DD）；金融领域选题时专业财经源（第一财经/界面/21财经）+0.5。
- 理由：拆词让“豆包大模型/QQ音乐”类真实选题命中更准；日期衰减让选题助手“近期有料”判断更真实；财经源加权贴合金融选题。
- 备注：纯 2-gram 召回保留（不丢召回）只是降权；`DOMAIN_TERMS` 可持续扩充；matched 非空时 2-gram 累加不封顶属既有行为。

## D-018 领域词表文件化 + 权重 + jieba 候选生成工具

- 日期：2026-08-15
- 背景：D-017 的领域词表硬编码在代码中、无权重、覆盖有限；用户要求词表独立 JSON、权重分级，并用爬虫（不耗 token）生成候选。
- 决策：
  1. 词表移到 `config/domain_terms.json`：科技/金融 → 子类 → 词:权重（2 强 / 1 弱）；`search.py` 启动时加载（`_load_domain_terms`），缺失回退内置精简词表。
  2. 权重语义：`domain_of` 加权投票（强 +2 / 弱 +1）；`_KNOWN_SUBTERMS` 只含强中文词参与拆词召回，弱词只参与领域判定。
  3. 新增依赖 jieba（清华镜像）；新增 `scripts/scan_domain_terms.py`：抓科技组（RSS 标题）与金融组（列表页标题），同口径 jieba 分词 + 文档频次 + log 比率判别，产出候选清单到 `docs/domain_terms_candidates.md`，人工审核后并入 JSON（不自动入库）。
- 首次审核结果（并入 7 词）：科技 agent/模型/智能体/GLM（强，2）、编程/数据（弱，1）；金融 亿元（弱，1）。智能体为 agent 的中文对应，人工补强；其余候选（新闻套话/栏目名/产品后缀）不收录。
- 理由：权重让“一票定领域”与拆词召回更准；同口径标题统计避免“标题+摘要 vs 标题”的频次失衡；候选清单可审计、可持续扩充。
- 备注：jieba 0.42.1 在 Python 3.14 有 SyntaxWarning（上游问题，不影响运行，脚本用 `-W ignore` 运行）；cnBeta RSS 偶发坏 XML（脚本已容错跳过）。

## D-019 智能体生态词扩充（第二轮审核）

- 日期：2026-08-15
- 背景：回归确认 `agent`/`智能体` 收录后，用户要求补充智能体生态相关更多实体，为下一阶段做准备。
- 决策：重跑 `scan_domain_terms.py` 后人工审核并入 7 词（科技/AI与大模型）：`harness`（候选判别力 2.30，DeepSeek Harness 为 agent 基础设施热点）；人工补强 `多智能体`/`MCP`/`Agentic`/`Copilot`（强，2）与 `工作流`/`RPA`（弱，1）。
- 理由：候选清单直接命中 + 人工补强近期科技媒体高频的智能体生态实体；弱词只参与领域判定。
- 备注：词表现状——科技 55 词 / 金融 48 词；英文词（MCP/Copilot/Agentic）不参与中文拆词，仅参与领域判定与标题匹配。
## D-020 报告质量验收修复：选文质量 + 搜索相关性收紧 + 抓取正文加长

- 日期：2026-08-16
- 背景：验收按 3→2→1→4 顺序推进，第 3 步“报告质量验收”重跑宁德时代报告时发现：① 编排层把早报/周记/汇总类聚合文章当材料，研究员提炼 0 事实仍继续出废报告（此前曾出“滴滴/巴西”幻觉）；② 搜索层把“融资/估值”等领域通用词当强词（+3），聚合文章靠摘要提及即可霸榜，选题预检虚报“有料 27 条”。
- 决策：
  1. 编排层（orchestration.py）：GATHER_SYSTEM 明确避开早报/晚报/周记/汇总/盘点类聚合文章；抓取后核心实体过滤（剔除领域词后要求“宁德时代”等实体命中标题/正文，否则跳过）；研究员 0 事实 → RuntimeError 中止并给出选题建议；代码层搜索次数上限 MAX_GATHER_SEARCHES=3（提示词“最多 2 次”不被模型遵守，需代码兜底防烧 token）。
  2. 搜索层（search.py）：课题已有实体词时，领域通用词（融资/估值/大模型…）降级为弱词（标题 +1.5 / 正文 +1.0，且不计入 matched，选题预检“真相关”必须命中核心实体）；聚合/盘点类标题（早报/周记/早知道/盘点/大事记…）分数封顶 1.0（含财经源 +0.5 与日期加成后仍 <2.0 真相关阈值）；词条尾部年份剥离（宁德时代2026年 → 宁德时代）减少“时代/20/26”类泛 2-gram；2-gram 总贡献封顶 1.0。
  3. 抓取正文：web.py 默认 max_chars 3000 → 6000（正文在 3000 字符内被截断导致有效内容与深埋实体丢失、被核心实体过滤误杀）；编排层研究员材料 body[:4000] → body[:6000]。
  4. 审查判定渲染：reviewer 偶发返回 schema 外值（如 unknown），报告统一渲染为“通过（原始判定：unknown）”，未触发修订视为通过。
- 理由：先保证“不出废报告、不烧 token”，再提升召回与诚实判定；搜索层把聚合文章从“真相关”剔除后，选题预检才可信（宁德时代 → 没料，豆包 → 有料）。
- 验收：豆包 vs 通义千问报告端到端通过——19 条事实全部带来源、SWOT/结论引用事实编号、审查未发现幻觉；宁德时代选题预检如实判“没料”，不再误导用户开跑。
- 备注：宁德时代“没料”的另一半原因是财经源只有首页列表、非头条词搜不到，属第 1 步“财经源栏目页细化”范围；重跑发现 geekpark 等长篇正文实体深埋 4000+ 字符，6000 上限仍可能漏，后续可按需再提。
## D-021 领域定制报告维度 + 研究员空事实重试 + 模型切换 qwen3.7-plus

- 日期：2026-08-16
- 背景：第 2 步“报告维度按领域定制”落地时，研究员（qwen3.5-omni-plus）连续 0 事实：模型实际返回了合法 JSON，但 `chat_json._extract_json` 解析失败回退 `_raw`（长 JSON 易被 max_tokens 截断 / 模型偶发格式漂移）。排查中百炼对 qwen3.7-flash 与 qwen3.5-omni-plus 均返回 403 Free quota exhausted，用户指定切换到 qwen3.7-plus。
- 决策：
  1. 领域定制维度（prompts.py）：新增 DOMAIN_DIMENSIONS（科技=技术路线/产品迭代/生态/商业化/人才组织；金融=市场规模/财务估值/监管政策/风险/资本开支融资；综合=两者；其他=市场/竞争/风险/趋势）；新增 researcher_system(domain)/analyst_system(domain) 按领域补充关注维度；orchestration 用 domain_of(topic) 判定领域并传给研究员/分析师，报告头部显示“领域：X”。
  2. 研究员空事实重试：run_pipeline 中研究员首次返回 0 事实时自动重试一次（防偶发格式漂移浪费整轮报告），仍为空才中止。
  3. 模型切换：LLM_MODEL_FLASH 与 LLM_MODEL_PLUS 统一改为 qwen3.7-plus（.env / .env.example / config.py / AGENTS.md 同步；.env 为本地私有不入库）。
- 理由：领域维度让科技/金融报告的 SWOT 与结论更贴合领域分析框架；重试机制低成本防偶发失败；模型切换因免费额度耗尽（403 insufficient_quota），qwen3.7-plus 实测可用。
- 验收：豆包 vs 通义千问在 qwen3.7-plus 下端到端通过——24 条事实全带来源，SWOT/结论覆盖科技领域维度（技术路线/生态/商业化/信任与成本），审查员全部 found、整体通过，报告头部显示“领域：科技”。
- 备注：qwen3.7-plus 跑收集阶段（原 flash 角色）耗时约 285s（flash 约 80s），后续若有更便宜的 flash 模型可换回，保持“高频机械活用便宜模型”的设计。
