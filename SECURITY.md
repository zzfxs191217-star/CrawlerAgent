# 安全说明（Security）

## 报告漏洞

发现安全漏洞请通过 GitHub Issue（选择 Security 模板）或仓库维护者邮箱私密提交，请勿在公开渠道张贴密钥或敏感信息。

## 本项目的安全约定

- API 密钥只存放在本地 `.env` 文件，且已被 `.gitignore` 排除，永不提交。
- 克隆后请复制 `.env.example` 为 `.env` 并填入自己的密钥与端点。
- 若怀疑密钥泄露，请立即在百炼控制台轮换（创建新 Key 并停用旧 Key）。
- 本仓库不包含任何真实密钥、Token 或账号信息。